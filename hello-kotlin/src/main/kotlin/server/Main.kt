// Main entry point for Kotlin audio stream server - delegates to AudioServerApplication.

package server

/**
 * Main entry point - delegates to AudioServerApplication
 */
fun main(args: Array<String>) {
    AudioServerApplication.main(args)
}

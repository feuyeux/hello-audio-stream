/**
 * Audio Stream Cache Client - TypeScript Implementation
 * Legacy entry point - redirects to AudioClientApplication
 * @deprecated Use client/AudioClientApplication.ts instead
 */

import "./client/AudioClientApplication";
